<?xml version="1.0" ?>
<!DOCTYPE msxconfig SYSTEM 'msxconfig2.dtd'>
<msxconfig>

  <info>
    <name>V9968 Verilator</name>
    <manufacturer>openMSX Team</manufacturer>
    <code/>
    <release_year>2025</release_year>
    <description>V9968 Verilator integration extension (Phase 1 stub).
Registers V9968VerilatorDevice in the openMSX device system.
When loaded, the device hooks into VDP IO ports and VSYNC events
and renders gradient frames via its own PostProcessor pipeline.</description>
    <type>debug tool</type>
  </info>

  <devices>

    <V9968VerilatorDevice id="V9968Verilator">
      <io base="0x98" num="4" type="IO"/>
    </V9968VerilatorDevice>

  </devices>

</msxconfig>
